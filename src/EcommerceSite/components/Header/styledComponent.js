import styled from '@emotion/styled';
import tw from 'tailwind.macro';


const HeaderContainer=styled.div`
${tw` 
flex 
justify-between
mb-4
mt-0
`}`;

const ProductsCount=styled.div`
${tw` 
w-auto
h-8
`}`;



export {HeaderContainer,ProductsCount}